{
  "name": "design-token-transformer",
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@ctrl/tinycolor": "^3.4.0"
  },
  "devDependencies": {
    "deepmerge": "^4.3.1",
    "style-dictionary": "^4.0.0"
  },
  "scripts": {
    "build": "node_modules/.bin/style-dictionary build --config ./config.js",
    "clean": "rm -rf build"
  }
}
